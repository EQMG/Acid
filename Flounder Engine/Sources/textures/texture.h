#pragma once

#include <string>

namespace flounder {
	class texture
	{
	public:
		/// <summary>
		/// A new OpenGL texture object.
		/// </summary>
		texture();

		/// <summary>
		/// Deconstructor for the texture object.
		/// </summary>
		~texture();
	};
}
